# ============================================
# Development Environment Variables
# ============================================
#
# This file contains development-specific environment variables.
# Copy this to .env.local and customize for your local setup.
#
# Usage:
#   cp .env.dev .env.local
#   # Edit .env.local with your settings
#   export $(cat .env.local | xargs)
#   make dev-start
# ============================================

# Development workspace (test project directory)
WORKSPACE_DIR=./test-project

# Container name for development
CONTAINER_NAME=codekiwi-dev

# Ports for development (avoid conflicts with production instances)
WEB_PORT=8080
DEV_PORT=3000

# Install template automatically for empty directories
INSTALL_TEMPLATE=yes

# Development-specific image tag
VERSION=dev

# Auth directory (shared with production)
AUTH_DIR=${HOME}/.codekiwi/opencode
